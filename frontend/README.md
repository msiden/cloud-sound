### Take home assigment frontend

This app is started with `docker compose up` in the root folder, but you can also run a standalone webpack dev server with hot reload if you run:

```
npm install
npm run serve
```
